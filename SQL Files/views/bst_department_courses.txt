CREATE OR REPLACE VIEW bst_department_courses AS select c.code AS 'course code',c.name as 'course name',c.credits from courses c INNER JOIN departments d ON c.department = d.id WHERE d.title='Bio-science technology';